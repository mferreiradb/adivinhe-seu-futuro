const = variável que não pode receber um valor após a definição do primeiro
let = variável que pode receber um valor após a definição do primeiro
array = listas
objetos = conjunto de propriedades e valores